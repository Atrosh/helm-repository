{{- if .Values.enabled }}
{{- if .Values.httproutes.enabled }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "spring-app.fullname" . }}
  labels:
{{- include "spring-app.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ .Values.httproutes.gateway.name }}
      namespace: {{ .Values.httproutes.gateway.namespace }}
  rules:
    - matches:
{{- range .Values.httproutes.paths }}
        - path:
            type: {{ .pathType | default "PathPrefix" }}
            value: {{ .path | quote }}
{{- end }}
{{- with .Values.httproutes.filters }}
      filters:
{{- toYaml . | nindent 8 }}
{{- end }}
      backendRefs:
        - name: {{ include "spring-app.fullname" $ }}
          port: {{ $.Values.service.port | quote }}
      timeouts:
        request: {{ .Values.httproutes.timeout | quote }}
{{- end }}
{{- end }}
